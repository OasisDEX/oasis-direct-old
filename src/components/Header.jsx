import React from 'react'

const Header = ({text}) => (
	<header className="Direct-Header Direct-Modifier--no-border">
		<h4 className="Direct-Modifier--uppercase">{text}</h4>
	</header>
);

export default Header;